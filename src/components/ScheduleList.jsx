import ScheduleItem from "./ScheduleItem"; export default function ScheduleList({ schedule, completed, onToggle, onTimer, }) { return (<section className="glass rounded-3xl p-4 md:p-6"> <div className="mb-5 flex items-center justify-between"> <div> <h2 className="text-lg font-bold"> برنامه امروز </h2> <p className="mt-1 text-xs text-slate-500"> هر فعالیت را پس از انجام علامت بزن. </p> </div> <span className="rounded-full bg-gold-500/10 px-3 py-1 text-xs text-gold-400"> {schedule.length} فعالیت </span> </div> <div className="space-y-2.5"> {schedule.map((item) => (<ScheduleItem key={item.id} item={item} completed={completed[item.id] || false} onToggle={onToggle} onTimer={onTimer} />))} </div> </section>); }